/**
 * @file
 * sdc_components behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.sdcComponents = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
